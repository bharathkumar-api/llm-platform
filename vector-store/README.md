# Vector Store

Place scripts and config here for:
- Chroma or Pinecone index creation
- Document ingestion (code, runbooks, KB)
- Periodic embedding pipelines

Your agent-service and llm-gateway can call into this layer for RAG.
